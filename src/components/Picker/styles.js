import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    pickerView:{
        marginTop: 20,
        backgroundColor: '#FFF',
        alignItems: "center",
        width: '90%'
    }
})

export default styles;